# Nodejs Deps
/node_modules/

# Generated package output
/artifacts/
/dist/
/npm/
# Perry vendor prebuilts are produced in CI and packed into the npm tarball;
# keep them out of git (large binaries).
/prebuilt/

#Added by cargo
/target
/dist